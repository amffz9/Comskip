#pragma once

struct RecordingContext;

void InitComSkip(RecordingContext& context);

// Set up the detector state for a recording before frames are processed.
void InitComSkip(RecordingContext& context);
