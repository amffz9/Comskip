cmake_minimum_required(VERSION 3.25)
project(Comskip VERSION 0.83.1 LANGUAGES CXX)
set(CMAKE_CXX_STANDARD 23)
set(CMAKE_CXX_STANDARD_REQUIRED ON)
set(CMAKE_CXX_EXTENSIONS OFF)
include(CTest)
add_library(reference_comparison src/detection/reference_comparison.cpp)
add_library(frame_mask src/detection/frame_mask.cpp)
add_library(scene_sampling src/detection/scene_sampling.cpp)
add_library(logo_geometry src/detection/logo_geometry.cpp)
target_include_directories(logo_geometry PUBLIC src/detection)
target_include_directories(scene_sampling PUBLIC src/detection)
target_include_directories(frame_mask PUBLIC src/detection)
target_include_directories(reference_comparison PUBLIC src)
find_package(rapidcsv CONFIG REQUIRED)
add_library(input_text src/input/reference_file.cpp src/input/frame_record.cpp)
target_include_directories(input_text PUBLIC src)
target_link_libraries(input_text PRIVATE rapidcsv::rapidcsv)
if(BUILD_TESTING)
    find_package(GTest CONFIG REQUIRED)
    include(GoogleTest)
    add_executable(text_input_test tests/input/text_input_test.cpp)
    target_link_libraries(text_input_test PRIVATE input_text GTest::gtest_main)
    gtest_discover_tests(text_input_test)
    add_executable(reference_comparison_test tests/detection/reference_comparison_test.cpp)
    target_link_libraries(reference_comparison_test PRIVATE reference_comparison GTest::gtest_main)
    gtest_discover_tests(reference_comparison_test)
    add_executable(frame_mask_test tests/detection/frame_mask_test.cpp)
    target_link_libraries(frame_mask_test PRIVATE frame_mask GTest::gtest_main)
    gtest_discover_tests(frame_mask_test)
    add_executable(logo_sampling_test tests/detection/logo_sampling_test.cpp)
    target_include_directories(logo_sampling_test PRIVATE src)
    target_link_libraries(logo_sampling_test PRIVATE GTest::gtest_main)
    gtest_discover_tests(logo_sampling_test)
    add_executable(scene_sampling_test tests/detection/scene_sampling_test.cpp)
    target_link_libraries(scene_sampling_test PRIVATE scene_sampling GTest::gtest_main)
    gtest_discover_tests(scene_sampling_test)
    add_executable(logo_geometry_test tests/detection/logo_geometry_test.cpp)
    target_link_libraries(logo_geometry_test PRIVATE logo_geometry GTest::gtest_main)
    gtest_discover_tests(logo_geometry_test)
endif()
add_library(platform_paths src/platform/search_path.cpp src/platform/analysis_policy.cpp)
target_include_directories(platform_paths PUBLIC src/platform)
find_package(Threads REQUIRED)
option(COMSKIP_BUILD_APP "Build the FFmpeg application" ON)
option(COMSKIP_BUILD_GUI "Build the SDL review interface" OFF)
option(COMSKIP_DONATOR "Enable full-speed decoding" ON)
file(READ "${CMAKE_CURRENT_SOURCE_DIR}/config/defaults.ini" COMSKIP_DEFAULT_INI)
configure_file(cmake/config_defaults.h.in generated/config_defaults.h @ONLY)
set_property(DIRECTORY APPEND PROPERTY CMAKE_CONFIGURE_DEPENDS config/defaults.ini)
find_package(SimpleIni CONFIG QUIET)
if(NOT TARGET SimpleIni::SimpleIni)
    # Distribution packages can provide the header without a CMake package.
    find_path(SIMPLEINI_INCLUDE_DIR SimpleIni.h REQUIRED)
    add_library(SimpleIni::SimpleIni INTERFACE IMPORTED)
    set_target_properties(SimpleIni::SimpleIni PROPERTIES
        INTERFACE_INCLUDE_DIRECTORIES "${SIMPLEINI_INCLUDE_DIR}"
        INTERFACE_COMPILE_DEFINITIONS SI_NO_CONVERSION)
    find_library(SIMPLEINI_LIBRARY NAMES simpleini)
    if(SIMPLEINI_LIBRARY)
        set_property(TARGET SimpleIni::SimpleIni PROPERTY
            INTERFACE_LINK_LIBRARIES "${SIMPLEINI_LIBRARY}")
    endif()
endif()
add_library(comskip_config src/config/ini.cpp src/config/settings_value.cpp src/config/profile.cpp)
target_link_libraries(comskip_config PRIVATE SimpleIni::SimpleIni)
target_include_directories(comskip_config PUBLIC src/config PRIVATE "${CMAKE_CURRENT_BINARY_DIR}/generated")
file(READ config/locales/en.ini COMSKIP_ENGLISH_CATALOG)
file(READ config/locales/es.ini COMSKIP_SPANISH_CATALOG)
configure_file(cmake/localization_catalogs.h.in generated/localization_catalogs.h @ONLY)
set_property(DIRECTORY APPEND PROPERTY CMAKE_CONFIGURE_DEPENDS config/locales/en.ini config/locales/es.ini)
add_library(comskip_localization src/localization/translator.cpp)
target_include_directories(comskip_localization PUBLIC src/localization PRIVATE "${CMAKE_CURRENT_BINARY_DIR}/generated")
target_link_libraries(comskip_localization PUBLIC comskip_config)
find_package(pugixml CONFIG REQUIRED)
add_library(output_text src/output/edl.cpp src/output/xml_cutlists.cpp src/output/live_xml.cpp src/output/plist_cutlist.cpp)
target_include_directories(output_text PUBLIC src/output src)
target_link_libraries(output_text PUBLIC pugixml::pugixml)

add_library(review_window src/ui/review_window.cpp)
target_include_directories(review_window PUBLIC src/ui)
target_compile_definitions(review_window PUBLIC
    COMSKIP_BUILD_GUI=$<BOOL:${COMSKIP_BUILD_GUI}>
    COMSKIP_DEFAULT_FONT_FILE="${CMAKE_CURRENT_SOURCE_DIR}/assets/fonts/NotoSans.ttf")
if(COMSKIP_BUILD_GUI)
    find_package(SDL2 CONFIG REQUIRED)
    find_package(SDL2_ttf CONFIG REQUIRED)
    target_link_libraries(review_window PRIVATE SDL2::SDL2 SDL2_ttf::SDL2_ttf)
endif()

add_library(commercial_length src/detection/commercial_length.cpp src/detection/weighted_scores.cpp)
target_include_directories(commercial_length PUBLIC src/detection)
target_link_libraries(commercial_length PUBLIC comskip_config)
if(BUILD_TESTING)
    find_package(GTest CONFIG REQUIRED)
    include(GoogleTest)
    add_executable(search_path_test tests/platform/search_path_test.cpp)
    target_link_libraries(search_path_test PRIVATE platform_paths GTest::gtest_main)
    gtest_discover_tests(search_path_test)
    if(WIN32)
        add_executable(analysis_policy_test tests/platform/analysis_policy_test.cpp)
        target_link_libraries(analysis_policy_test PRIVATE platform_paths GTest::gtest_main)
        gtest_discover_tests(analysis_policy_test)
    endif()
    add_executable(weighted_scores_test tests/detection/weighted_scores_test.cpp)
    target_link_libraries(weighted_scores_test PRIVATE commercial_length GTest::gtest_main)
    gtest_discover_tests(weighted_scores_test)
    add_executable(argument_snapshot_test tests/platform/argument_snapshot_test.cpp)
    target_include_directories(argument_snapshot_test PRIVATE src/app)
    target_link_libraries(argument_snapshot_test PRIVATE GTest::gtest_main)
    gtest_discover_tests(argument_snapshot_test)
    add_executable(file_resources_test tests/platform/file_resources_test.cpp)
    target_include_directories(file_resources_test PRIVATE src/platform)
    target_link_libraries(file_resources_test PRIVATE GTest::gtest_main)
    gtest_discover_tests(file_resources_test)
    add_executable(buffer_growth_test tests/detection/buffer_growth_test.cpp)
    target_include_directories(buffer_growth_test PRIVATE src/detection)
    target_link_libraries(buffer_growth_test PRIVATE GTest::gtest_main)
    gtest_discover_tests(buffer_growth_test)
    add_executable(review_window_test tests/ui/review_window_test.cpp)
    target_link_libraries(review_window_test PRIVATE review_window GTest::gtest_main)
    if(COMSKIP_BUILD_GUI)
        target_link_libraries(review_window_test PRIVATE SDL2::SDL2)
    endif()
    gtest_discover_tests(review_window_test PROPERTIES ENVIRONMENT "SDL_VIDEODRIVER=dummy")
    add_executable(translator_test tests/localization/translator_test.cpp)
    target_link_libraries(translator_test PRIVATE comskip_localization GTest::gtest_main)
    target_compile_definitions(translator_test PRIVATE COMSKIP_SOURCE_DIR="${CMAKE_CURRENT_SOURCE_DIR}")
    gtest_discover_tests(translator_test)
    add_executable(edl_test tests/output/edl_test.cpp)
    target_link_libraries(edl_test PRIVATE output_text GTest::gtest_main)
    gtest_discover_tests(edl_test)
    add_executable(xml_cutlists_test tests/output/xml_cutlists_test.cpp)
    target_link_libraries(xml_cutlists_test PRIVATE output_text GTest::gtest_main)
    gtest_discover_tests(xml_cutlists_test)
    add_executable(live_xml_test tests/output/live_xml_test.cpp)
    target_link_libraries(live_xml_test PRIVATE output_text GTest::gtest_main)
    gtest_discover_tests(live_xml_test)
    add_executable(plist_cutlist_test tests/output/plist_cutlist_test.cpp)
    target_link_libraries(plist_cutlist_test PRIVATE output_text GTest::gtest_main)
    gtest_discover_tests(plist_cutlist_test)
    add_executable(commercial_length_test tests/detection/commercial_length_test.cpp)
    target_link_libraries(commercial_length_test PRIVATE commercial_length GTest::gtest_main)
    target_compile_definitions(commercial_length_test PRIVATE COMSKIP_SOURCE_DIR="${CMAKE_CURRENT_SOURCE_DIR}")
    gtest_discover_tests(commercial_length_test TEST_PREFIX default.)
    add_executable(scan_workers_test tests/platform/scan_workers_test.cpp)
    target_include_directories(scan_workers_test PRIVATE src/platform)
    target_link_libraries(scan_workers_test PRIVATE Threads::Threads GTest::gtest_main)
    gtest_discover_tests(scan_workers_test)
    add_executable(settings_test tests/config/settings_test.cpp)
    target_link_libraries(settings_test PRIVATE comskip_config GTest::gtest_main)
    gtest_discover_tests(settings_test)
    add_executable(settings_value_test tests/config/settings_value_test.cpp)
    target_link_libraries(settings_value_test PRIVATE comskip_config GTest::gtest_main)
    gtest_discover_tests(settings_value_test)
    add_executable(checked_format_test tests/platform/checked_format_test.cpp)
    target_include_directories(checked_format_test PRIVATE src/app)
    target_link_libraries(checked_format_test PRIVATE GTest::gtest_main)
    gtest_discover_tests(checked_format_test)
    add_executable(review_media_test tests/ui/review_media_test.cpp)
    target_include_directories(review_media_test PRIVATE src)
    target_link_libraries(review_media_test PRIVATE GTest::gtest_main)
    gtest_discover_tests(review_media_test)
    add_executable(review_intervals_test tests/ui/review_intervals_test.cpp)
    target_include_directories(review_intervals_test PRIVATE src)
    target_link_libraries(review_intervals_test PRIVATE GTest::gtest_main)
    gtest_discover_tests(review_intervals_test)
    add_executable(stalled_packet_counter_test tests/media/stalled_packet_counter_test.cpp)
    target_include_directories(stalled_packet_counter_test PRIVATE src)
    target_link_libraries(stalled_packet_counter_test PRIVATE GTest::gtest_main)
    gtest_discover_tests(stalled_packet_counter_test)
    add_executable(platform_test tests/platform/platform_test.cpp src/platform/platform.cpp)
    target_include_directories(platform_test PRIVATE src/platform)
    target_link_libraries(platform_test PRIVATE Threads::Threads GTest::gtest_main)
    gtest_discover_tests(platform_test)
    if(WIN32)
        add_executable(arguments_test tests/platform/arguments_test.cpp)
        target_include_directories(arguments_test PRIVATE src/app)
        target_link_libraries(arguments_test PRIVATE GTest::gtest_main)
        gtest_discover_tests(arguments_test)
    endif()
endif()

if(COMSKIP_BUILD_APP)
    find_path(FFMPEG_INCLUDE_DIR libavformat/avformat.h REQUIRED)
    foreach(component avformat avcodec avutil swscale swresample)
        find_library(${component}_LIBRARY NAMES ${component} REQUIRED)
        list(APPEND FFMPEG_LIBRARIES ${${component}_LIBRARY})
    endforeach()
    add_library(media_conversion src/media/audio_samples.cpp src/media/frame_conversion.cpp src/media/a53_caption_bridge.cpp)
    target_include_directories(media_conversion PUBLIC src/media ${FFMPEG_INCLUDE_DIR})
    target_link_libraries(media_conversion PUBLIC ${swresample_LIBRARY} ${swscale_LIBRARY} ${avutil_LIBRARY})
    add_library(caption_decoder src/media/caption_decoder.cpp)
    target_include_directories(caption_decoder PUBLIC src/media ${FFMPEG_INCLUDE_DIR})
    target_link_libraries(caption_decoder PUBLIC ${avcodec_LIBRARY} ${avutil_LIBRARY})
    add_library(subtitle_output src/media/subtitle_output.cpp)
    target_include_directories(subtitle_output PUBLIC src src/media ${FFMPEG_INCLUDE_DIR})
    target_link_libraries(subtitle_output PUBLIC caption_decoder pugixml::pugixml ${avformat_LIBRARY} ${avcodec_LIBRARY} ${avutil_LIBRARY})
    add_library(caption_session src/media/caption_session.cpp)
    target_include_directories(caption_session PUBLIC src src/media)
    target_link_libraries(caption_session PUBLIC subtitle_output media_conversion)
    add_library(subtitle_stream_decoder src/media/subtitle_stream_decoder.cpp)
    target_include_directories(subtitle_stream_decoder PUBLIC src src/media ${FFMPEG_INCLUDE_DIR})
    target_link_libraries(subtitle_stream_decoder PUBLIC caption_decoder ${avcodec_LIBRARY} ${avutil_LIBRARY})
    target_link_libraries(caption_session PUBLIC subtitle_stream_decoder)
    if(BUILD_TESTING)
        add_executable(media_conversion_test tests/media/audio_samples_test.cpp tests/media/frame_conversion_test.cpp)
        target_link_libraries(media_conversion_test PRIVATE media_conversion GTest::gtest_main)
        gtest_discover_tests(media_conversion_test)
        add_executable(a53_caption_bridge_test tests/media/a53_caption_bridge_test.cpp)
        target_link_libraries(a53_caption_bridge_test PRIVATE media_conversion GTest::gtest_main)
        gtest_discover_tests(a53_caption_bridge_test)
        add_executable(caption_decoder_test tests/media/caption_decoder_test.cpp)
        target_link_libraries(caption_decoder_test PRIVATE caption_decoder GTest::gtest_main)
        gtest_discover_tests(caption_decoder_test)
        add_executable(subtitle_output_test tests/media/subtitle_output_test.cpp)
        target_link_libraries(subtitle_output_test PRIVATE subtitle_output GTest::gtest_main)
        gtest_discover_tests(subtitle_output_test)
        add_executable(subtitle_stream_decoder_test tests/media/subtitle_stream_decoder_test.cpp)
        target_link_libraries(subtitle_stream_decoder_test PRIVATE subtitle_stream_decoder GTest::gtest_main)
        gtest_discover_tests(subtitle_stream_decoder_test)
        add_executable(standalone_caption_session_test tests/media/standalone_caption_session_test.cpp)
        target_link_libraries(standalone_caption_session_test PRIVATE caption_session GTest::gtest_main)
        gtest_discover_tests(standalone_caption_session_test)
    endif()
    find_path(ARGTABLE_INCLUDE_DIR argtable2.h REQUIRED)
    find_library(ARGTABLE_LIBRARY NAMES argtable2 REQUIRED)
    add_library(comskip_core STATIC
        src/detection/state.cpp
        src/detection/blocks.cpp
        src/detection/detection.cpp
        src/detection/scoring.cpp
        src/detection/length_matching.cpp
        src/detection/scene_analysis.cpp
        src/detection/logo.cpp
        src/detection/storage.cpp
        src/detection/captions.cpp
        src/detection/live.cpp
        src/ui/review.cpp
        src/config/legacy_settings.cpp
        src/app/runtime.cpp
        src/app/pixel_buffers.cpp
        src/app/csv_input.cpp
        src/output/cutlists.cpp
        src/output/diagnostics.cpp
        src/output/media_dump.cpp
        src/media/audio_analysis.cpp
        src/media/timing_diagnostics.cpp
        src/media/frame_timing.cpp
        src/media/mpeg2dec.cpp src/platform/platform.cpp src/output/xml_output_adapter.cpp)
    target_include_directories(comskip_core PUBLIC src/platform src/ui src/app ${FFMPEG_INCLUDE_DIR} ${ARGTABLE_INCLUDE_DIR})
    target_compile_definitions(comskip_core PRIVATE PROCESS_CC $<$<BOOL:${COMSKIP_DONATOR}>:DONATOR>)
    target_link_libraries(comskip_core PUBLIC logo_geometry scene_sampling frame_mask reference_comparison input_text commercial_length comskip_config comskip_localization output_text media_conversion caption_session ${FFMPEG_LIBRARIES}
        ${ARGTABLE_LIBRARY} Threads::Threads review_window platform_paths)
    add_executable(comskip src/app/main.cpp)
    target_link_libraries(comskip PRIVATE comskip_core)
    if(WIN32)
        if(MINGW)
            target_link_options(comskip PRIVATE -municode)
        endif()
        find_library(GETOPT_LIBRARY NAMES getopt REQUIRED)
        target_link_libraries(comskip_core PRIVATE ${GETOPT_LIBRARY})
        target_compile_definitions(comskip_core PRIVATE _CRT_SECURE_NO_WARNINGS)
    else()
        target_link_libraries(comskip_core PRIVATE m)
    endif()
    if(BUILD_TESTING)
        add_executable(inprocess_analysis_test tests/media/inprocess_analysis_test.cpp)
        add_executable(xml_export_test tests/output/xml_export_test.cpp)
        add_executable(live_xml_export_test tests/output/live_xml_export_test.cpp)
        target_link_libraries(live_xml_export_test PRIVATE comskip_core GTest::gtest_main)
        gtest_discover_tests(live_xml_export_test)
        target_link_libraries(xml_export_test PRIVATE comskip_core GTest::gtest_main)
        gtest_discover_tests(xml_export_test)
        target_link_libraries(inprocess_analysis_test PRIVATE comskip_core GTest::gtest_main)
        add_executable(xds_test tests/detection/xds_test.cpp)
        add_executable(diagnostics_test tests/app/diagnostics_test.cpp)
        target_link_libraries(diagnostics_test PRIVATE comskip_core GTest::gtest_main)
        gtest_discover_tests(diagnostics_test)
        add_executable(diagnostic_output_test tests/output/diagnostic_output_test.cpp)
        target_link_libraries(diagnostic_output_test PRIVATE comskip_core GTest::gtest_main)
        gtest_discover_tests(diagnostic_output_test)
        add_executable(caption_analysis_test tests/media/caption_analysis_test.cpp)
        target_link_libraries(caption_analysis_test PRIVATE comskip_core GTest::gtest_main)
        gtest_discover_tests(caption_analysis_test)
        add_executable(input_filename_test tests/media/input_filename_test.cpp)
        target_link_libraries(input_filename_test PRIVATE comskip_core GTest::gtest_main)
        gtest_discover_tests(input_filename_test)
        add_executable(warnings_test tests/detection/warnings_test.cpp)
        target_link_libraries(warnings_test PRIVATE comskip_core GTest::gtest_main)
        gtest_discover_tests(warnings_test)
        add_executable(cutscene_loading_test tests/detection/cutscene_loading_test.cpp)
        target_link_libraries(cutscene_loading_test PRIVATE comskip_core GTest::gtest_main)
        gtest_discover_tests(cutscene_loading_test)
        add_executable(cutlist_errors_test tests/output/cutlist_errors_test.cpp)
        target_link_libraries(cutlist_errors_test PRIVATE comskip_core GTest::gtest_main)
        gtest_discover_tests(cutlist_errors_test)
        add_executable(application_text_input_test tests/input/application_text_input_test.cpp)
        target_link_libraries(application_text_input_test PRIVATE comskip_core GTest::gtest_main)
        gtest_discover_tests(application_text_input_test)
        add_executable(reference_comparison_application_test tests/detection/reference_comparison_application_test.cpp)
        target_link_libraries(reference_comparison_application_test PRIVATE comskip_core rapidcsv::rapidcsv GTest::gtest_main)
        gtest_discover_tests(reference_comparison_application_test)
        add_executable(detection_blocks_test tests/detection/detection_blocks_test.cpp)
        target_link_libraries(detection_blocks_test PRIVATE comskip_core GTest::gtest_main)
        gtest_discover_tests(detection_blocks_test)
        add_executable(commercial_storage_test tests/detection/commercial_storage_test.cpp)
        target_link_libraries(commercial_storage_test PRIVATE comskip_core GTest::gtest_main)
        gtest_discover_tests(commercial_storage_test)
        add_executable(scene_sampling_application_test tests/detection/scene_sampling_application_test.cpp)
        target_link_libraries(scene_sampling_application_test PRIVATE comskip_core GTest::gtest_main)
        gtest_discover_tests(scene_sampling_application_test)
        add_executable(logo_geometry_application_test tests/detection/logo_geometry_application_test.cpp)
        target_link_libraries(logo_geometry_application_test PRIVATE comskip_core GTest::gtest_main)
        gtest_discover_tests(logo_geometry_application_test)
        add_executable(cli_paths_test tests/media/cli_paths_test.cpp)
        target_link_libraries(cli_paths_test PRIVATE comskip_core GTest::gtest_main)
        gtest_discover_tests(cli_paths_test)
        add_executable(caption_packets_test tests/detection/caption_packets_test.cpp)
        target_link_libraries(caption_packets_test PRIVATE comskip_core GTest::gtest_main)
        gtest_discover_tests(caption_packets_test)
        add_executable(frame_timing_test tests/media/frame_timing_test.cpp)
        target_link_libraries(frame_timing_test PRIVATE comskip_core GTest::gtest_main)
        gtest_discover_tests(frame_timing_test)
        add_executable(playback_warnings_test tests/media/playback_warnings_test.cpp)
        target_link_libraries(playback_warnings_test PRIVATE comskip_core GTest::gtest_main)
        gtest_discover_tests(playback_warnings_test)
        add_executable(timing_output_test tests/media/timing_output_test.cpp)
        target_link_libraries(timing_output_test PRIVATE comskip_core GTest::gtest_main)
        gtest_discover_tests(timing_output_test)
        add_executable(executable_mode_test tests/ui/executable_mode_test.cpp)
        target_link_libraries(executable_mode_test PRIVATE comskip_core GTest::gtest_main)
        gtest_discover_tests(executable_mode_test PROPERTIES TIMEOUT 30)
        add_executable(pixel_buffers_test tests/detection/pixel_buffers_test.cpp)
        target_link_libraries(pixel_buffers_test PRIVATE comskip_core GTest::gtest_main)
        gtest_discover_tests(pixel_buffers_test)
        target_link_libraries(xds_test PRIVATE comskip_core GTest::gtest_main)
        gtest_discover_tests(xds_test)
        gtest_discover_tests(inprocess_analysis_test PROPERTIES TIMEOUT 120)
        find_package(Python3 COMPONENTS Interpreter REQUIRED)
        add_test(NAME cli_localization COMMAND ${Python3_EXECUTABLE}
            ${CMAKE_CURRENT_SOURCE_DIR}/tests/localization/cli_localization.py $<TARGET_FILE:comskip>)
        add_test(NAME cli_locale COMMAND ${Python3_EXECUTABLE}
            "${CMAKE_CURRENT_SOURCE_DIR}/tests/localization/cli_locale.py" $<TARGET_FILE:comskip>)
        add_test(NAME media_smoke COMMAND ${Python3_EXECUTABLE}
            "${CMAKE_CURRENT_SOURCE_DIR}/tests/media/media_smoke.py" $<TARGET_FILE:comskip>
            "${CMAKE_CURRENT_BINARY_DIR}/integration")
        set_tests_properties(media_smoke PROPERTIES TIMEOUT 120)
        add_test(NAME commercial_intervals COMMAND ${Python3_EXECUTABLE}
            "${CMAKE_CURRENT_SOURCE_DIR}/tests/media/commercial_intervals.py" $<TARGET_FILE:comskip>
            "${CMAKE_CURRENT_BINARY_DIR}/commercial-intervals")
        set_tests_properties(commercial_intervals PROPERTIES TIMEOUT 120)
        find_program(FFMPEG_EXECUTABLE NAMES ffmpeg
            HINTS "${VCPKG_INSTALLED_DIR}/${VCPKG_TARGET_TRIPLET}/tools/ffmpeg"
            PATH_SUFFIXES tools/ffmpeg)
        if(FFMPEG_EXECUTABLE)
            add_test(NAME missing_timestamps COMMAND ${Python3_EXECUTABLE}
                "${CMAKE_CURRENT_SOURCE_DIR}/tests/media/missing_timestamps.py" $<TARGET_FILE:comskip>
                "${FFMPEG_EXECUTABLE}" "${CMAKE_CURRENT_BINARY_DIR}/timestamps")
            set_tests_properties(missing_timestamps PROPERTIES TIMEOUT 120)
            add_test(NAME standalone_subtitles COMMAND ${Python3_EXECUTABLE}
                "${CMAKE_CURRENT_SOURCE_DIR}/tests/media/standalone_subtitles.py" $<TARGET_FILE:comskip>
                "${FFMPEG_EXECUTABLE}" "${CMAKE_CURRENT_BINARY_DIR}/standalone")
            set_tests_properties(standalone_subtitles PROPERTIES TIMEOUT 120)
            add_test(NAME media_formats COMMAND ${Python3_EXECUTABLE}
                "${CMAKE_CURRENT_SOURCE_DIR}/tests/media/media_formats.py" $<TARGET_FILE:comskip>
                "${FFMPEG_EXECUTABLE}" "${CMAKE_CURRENT_BINARY_DIR}/formats")
            set_tests_properties(media_formats PROPERTIES TIMEOUT 180)
            add_test(NAME media_recovery COMMAND ${Python3_EXECUTABLE}
                "${CMAKE_CURRENT_SOURCE_DIR}/tests/media/media_recovery.py" $<TARGET_FILE:comskip>
                "${FFMPEG_EXECUTABLE}" "${CMAKE_CURRENT_BINARY_DIR}/recovery")
            set_tests_properties(media_recovery PROPERTIES TIMEOUT 180)
        else()
            message(STATUS "ffmpeg executable unavailable: media_formats test disabled")
        endif()
    endif()
endif()
